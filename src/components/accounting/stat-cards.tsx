"use client";
import type { RangePreset } from "@/lib/accounting/date-ranges";
export default function StatCards({ range }: { range: RangePreset }) {
  return <div className="text-xs text-muted-foreground">Stat cards stub ({range}) — Task 22</div>;
}
