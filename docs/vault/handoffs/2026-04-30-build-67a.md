---
date: 2026-04-30
build_id: 67a
session_type: focused
machine: Eric (TheLaunchPad)
related: ["[[build-14a]]", "[[build-14d]]", "[[build-17c]]", "[[build-18a]]", "[[build-66]]"]
---

# Build 67a Handoff — 2026-04-30

## What shipped this session

- **Spec doc** for Build 67a (Estimates & Invoices Foundation): [docs/superpowers/specs/2026-04-30-build-67a-estimates-foundation-design.md](../superpowers/specs/2026-04-30-build-67a-estimates-foundation-design.md). Resolves six conflicts between the v1.0 build guide and current platform state (build label collision 18a → 67a, single-tenant → multi-tenant, drop-and-rebuild → ALTER, deprecated `user_permissions` → `user_organization_permissions`, broken `/mnt/project/` paths, wrong migration naming convention). Commit `0fc5f18`.
- **Implementation plan** at [docs/superpowers/plans/2026-04-30-build-67a-estimates-foundation.md](../superpowers/plans/2026-04-30-build-67a-estimates-foundation.md). Tasks 1–9 fully detailed with exact code blocks; Tasks 10–30 outlined as milestones but not expanded yet.
- **Build guide v1.1** at `~/Downloads/Nookleus-Estimates-Invoices-Build-Guide-v1.md` (Eric's local copy, not in repo). Build labels 18a/b/c → 67a/b/c throughout. v1.0 SQL drop-and-rebuild and v1.0 Claude Code prompts wrapped in `ARCHIVED v1.0 (DO NOT RUN)` blocks so future Claudes don't grab the wrong version.
- **Schema migration** applied to prod (`rzzprgidqbnqcdupmpfe`) and committed: [supabase/migration-build67a-estimates-foundation.sql](../../../supabase/migration-build67a-estimates-foundation.sql). Drops legacy `line_items` (0 rows). Creates 7 new tables — `item_library`, `estimates`, `estimate_sections`, `estimate_line_items`, `estimate_templates`, `pdf_presets`, `invoice_sections` — all org-scoped with `tenant_isolation` RLS. Alters `invoices` in place (14 new columns + status check refresh + `UNIQUE(job_id, sequence_number)`). Alters `invoice_line_items` in place (3 new columns: `section_id`, `library_item_id`, `unit`). Adds two atomic numbering RPCs (`generate_estimate_number`, `generate_invoice_number`). Extends `set_default_permissions` with 12 new keys, backfilled across all existing memberships. Commit `f626ec8`.
- **Pre-migration cleanup:** deleted `INV-2026-0001` ($0.07 Build 17 test artifact) + its 2 `invoice_line_items` rows out-of-band so `sequence_number NOT NULL` could land cleanly. No commit (DB-only).
- **TypeScript types** added to `src/lib/types.ts`: `Estimate`, `EstimateSection`, `EstimateLineItem`, `ItemLibraryItem`, `EstimateWithContents`, `EstimateTemplate`, `TemplateItem`, `PdfPreset`, plus the three string-literal unions. Existing legacy `Invoice` / `LineItem` interfaces (referring to dropped `line_items` table) left untouched — reworked in 67b. Commit `636be2e`.
- **Currency helper** at [src/lib/format.ts](../../../src/lib/format.ts): `formatCurrency(n)` + `round2(n)`. Commit `6e489f0`.
- **`npm install` run in this worktree** — 1165 packages added. Worktree was missing `node_modules` (the SessionStart hook only syncs `.env.local`).

## What's next

In priority order:

1. **Write part-2 of the plan** covering Tasks 10–30 (API routes 4–8, Item Library admin page + components, estimate creation flow, estimate builder UI ~10 components, drag-and-drop, auto-save, read-only view, job-detail integration, sidebar nav, final audit). Tasks 1–9 are already detailed in part-1.
2. **Resume SDD execution at Task 5** — `src/lib/estimates.ts` (recalc + numbering wrapper + loaded fetch). Code is already fully written in plan part-1.
3. Continue through Tasks 5–14 (server libs + 8 API routes) in one session.
4. Then 2–3 more sessions for the builder UI components (Tasks 15–30).

## Decisions locked

- **Build label:** 67a / 67b / 67c (not 18a/b/c — that's already shipped multi-tenant infra). Eric: "option b."
- **Migration strategy for invoices:** ALTER, not drop. 35 source files reference `invoices` / `invoice_line_items`; QB-sync triggers + Stripe webhooks depend on them. Eric: "option a."
- **Multi-tenant from day 1:** every new table has `organization_id NOT NULL` + tenant-scoped RLS. Migration naming follows existing `supabase/migration-build<NN>-<name>.sql` convention. Permissions go to `user_organization_permissions` via `set_default_permissions(p_user_organization_id, p_role)`. Eric: "yes lock them in."
- **`INV-2026-0001` fate:** delete out-of-band before migration, skip backfill. Eric: "delete and skip backfill."
- **`xactimate_code` column kept as-is** (8 source files reference it; renaming = churn). Surfaced as `code` in TS via mapper at the API-route boundary. Same pattern as `total_amount` ↔ `total`. Decision made during migration retry — no explicit user vote, but follows the ALTER-not-rename principle Eric already approved.
- **Server-side perm check:** use existing [src/lib/permissions-api.ts:21](../../../src/lib/permissions-api.ts) `requirePermission(supabase, key)` — admin bypass via `user_organizations.role === 'admin'` matches client-side `hasPermission`. No new helper.
- **Crew Lead default permissions:** all view + create + edit + send + convert; no `manage_*`. Matches existing crew_lead pattern for accounting/contracts.

## Open threads

- **Tasks 5–30 of Build 67a** (26 of 30 tasks, ~87% remaining). Server libs (estimates.ts, item-library.ts) → 8 API routes → Item Library admin page → estimate creation flow → estimate builder UI (~10 components: header, sections, line items, dialog, totals, drag-and-drop, auto-save, save indicator) → read-only view → job-detail integration → sidebar nav → final audit.
- **Plan part-2** (Tasks 10–30 detail) needs writing before SDD can dispatch them.
- **`payments` table has 5 orphaned rows** (no `invoice_id`) from earlier accounting builds. Not affected by 67a but worth noting — they pre-date the Stripe-Invoice-payment glue.
- Pre-existing carryovers (unchanged from previous session): Build 14j Maps-to UX rework; `job_custom_fields` rendering on `/jobs/[id]`; workspace switcher reload bug; Mac sessions for [[build-65b]] §5.2.A and [[build-65a]] TestFlight; `/api/notifications` 500s.

## Mechanical state

- **Branch:** `claude/ecstatic-cartwright-7d2405` (worktree at `.claude/worktrees/ecstatic-cartwright-7d2405`); main is at `03cbeec`. This branch is 4 commits ahead.
- **Commit at session end:** `6e489f0 feat(67a): add formatCurrency + round2 helpers in src/lib/format.ts`. The handoff itself is the next commit on top.
- **Uncommitted changes at handoff write:** 2 (this file + `00-NOW.md` updates).
- **Migrations applied this session:** `build67a_estimates_foundation` (in prod via Supabase MCP `apply_migration`). Verified post-apply: 7 new tables RLS-enabled, 2 RPCs callable, 14 settings keys (7 × 2 orgs), 12 perm keys × 2 admin memberships seeded, legacy `line_items` dropped.
- **Deployed to Vercel:** no — this session only laid the schema and type foundation. No UI or API changes are live yet (the API routes start in Task 7).

## Notes for next session

- **The plan file is the source of truth for Tasks 1–9.** [docs/superpowers/plans/2026-04-30-build-67a-estimates-foundation.md](../superpowers/plans/2026-04-30-build-67a-estimates-foundation.md) has the exact code, schema, and verification steps. For Tasks 10–30, only milestone outlines exist at the bottom of part-1 — write part-2 first.

- **`invoice_line_items` shape gotcha.** Build 38 added `sort_order` and `xactimate_code` to `invoice_line_items`; the v1.0 migration tried to ADD them again and failed transactionally. The retry only adds `section_id`, `library_item_id`, `unit`. Surface `xactimate_code` as `code` and `amount` as `total` in TS via a mapper at the API boundary in 67b — same pattern as `total_amount` ↔ `total` on `invoices`. **Always inspect existing column shape before writing an ALTER ADD.**

- **The migration's pre-flight is destructive but clean.** Eric must delete the test invoice + its 2 line items BEFORE the migration runs (or the `sequence_number SET NOT NULL` step fails). For 67a this happened out-of-band via Supabase MCP; for any future similar pattern, document the pre-flight delete next to the migration file.

- **SDD pattern works well for this size of build.** Subagent dispatches at ~50–80K tokens each. Pre-investing context in writing detailed plan tasks pays off — the implementer subagent gets exactly the code and verification steps it needs and finishes in 1–2 minutes. Use `general-purpose` subagent.

- **No test framework.** "Verification" = `npx tsc --noEmit` + Supabase MCP SQL inspection + manual preview via Claude Preview MCP (not Claude in Chrome). Per memory `project_no_test_framework`, tsc baseline was clean as of 2026-04-19 after `npm install`; this session re-ran `npm install` so the worktree's `node_modules` is fresh.

- **Where to embed the estimates list on the job page.** [src/components/job-detail.tsx](../../../src/components/job-detail.tsx) is the client component, NOT the page file. The page at `src/app/jobs/[id]/page.tsx` is a thin server-rendered wrapper. The Billing card lives around line 480 of `job-detail.tsx`; embed `<EstimatesInvoicesSection jobId={job.id} />` between Billing and any Files section.

- **Existing helpers to reuse:**
  - `requirePermission(supabase, key)` from [src/lib/permissions-api.ts:21](../../../src/lib/permissions-api.ts:21)
  - `getActiveOrganizationId(supabase)` from [src/lib/supabase/get-active-org.ts:40](../../../src/lib/supabase/get-active-org.ts:40)
  - `createServerSupabaseClient()` from `@/lib/supabase-server`
  - Tiptap editor: default export at [src/components/tiptap-editor.tsx](../../../src/components/tiptap-editor.tsx)
  - DnD pattern: copy from [src/components/form-builder/canvas.tsx](../../../src/components/form-builder/canvas.tsx) (Build 14j)
  - Auto-save pattern: copy from [src/components/form-builder/use-form-config.ts](../../../src/components/form-builder/use-form-config.ts) (Build 14j)

- **Build guide v1.1** lives in Eric's `~/Downloads/`, not the repo. Don't try to read it from a fresh worktree — it's not there. The in-repo source of truth is the spec doc + plan.

- **Branch state at handoff.** This worktree branch (`claude/ecstatic-cartwright-7d2405`) is 4 commits ahead of `main` (which is at `03cbeec`, the 14j handoff). Next session has two reasonable paths: (a) keep working on this branch and PR to main when 67a is fully done; (b) merge the schema + types + format helper to main now (since the migration is already in prod, main is technically behind reality), then start a fresh worktree for the rest of 67a. Recommendation: (a) — keep the build-cohesive history on one branch.

## Links

- Spec: [docs/superpowers/specs/2026-04-30-build-67a-estimates-foundation-design.md](../superpowers/specs/2026-04-30-build-67a-estimates-foundation-design.md)
- Plan: [docs/superpowers/plans/2026-04-30-build-67a-estimates-foundation.md](../superpowers/plans/2026-04-30-build-67a-estimates-foundation.md)
- Migration: [supabase/migration-build67a-estimates-foundation.sql](../../../supabase/migration-build67a-estimates-foundation.sql)
- Multi-tenant context: [[build-18a]] (the build number 67a explicitly avoids re-using)
- Permission system context: [[build-48]] (`user_organization_permissions` and `set_default_permissions` shape)
- Previous handoff: [[2026-04-30-build-14j]]
- Current state: [[00-NOW]]
